{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./dist", "./.next"],
      "dependsOn": ["codegen"]
    },
    "codegen": {
      "outputs": ["./src/**/*.gen.*"]
    },
    "fix:eslint": {
      "cache": false
    },
    "lint:eslint": {
      "env": ["CHECK_TEMPORARILY_DISABLED_RULES"],
      "outputs": []
    },
    "lint:tsc": {
      "outputs": []
    },
    "@hashintel/hash-graph#codegen": {
      "dependsOn": ["@hashintel/status#build"],
      "outputs": ["lib/graph/src/api/gen/**"]
    }
  }
}
